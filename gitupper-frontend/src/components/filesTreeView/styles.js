import styled from "styled-components";

export const TreeItemText = styled.span`
  font-style: normal;
  font-weight: 500;
  font-size: 14px;
  line-height: 22px;

  /* Cores Texto/TextBlack */

  color: ${(props) => props.theme.colors.blackLight};
`;
